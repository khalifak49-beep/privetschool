@model ValueTuple<string, string>

<div class="d-flex justify-content-between align-items-start gap-3 py-2 border-bottom">
    <span class="text-muted-2 fs-sm flex-shrink-0">@Model.Item1</span>
    <span class="fw-semibold fs-sm text-end">@Model.Item2</span>
</div>
